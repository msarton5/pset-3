const readlineSync = require("readline-sync");

const Month = Number(readlineSync.question("\nEnter a month: "));

if (Number.isNaN(Amount)) {
    console.log("Invalid")
} else if (Month = "jan") {
    console.log("Invalid")
} else {
